import { db } from '@/lib/db';import { notFound } from 'next/navigation';
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;let p:any;try{const rows=await db()`select id,name,description,price_cents,currency,stock from products where slug=${slug} and active=true limit 1`;p=rows[0]}catch{}if(!p)notFound();return <main className="wrap section"><div className="card"><h1>{p.name}</h1><p>{p.description}</p><div className="price">{(p.price_cents/100).toLocaleString('it-IT',{style:'currency',currency:p.currency})}</div><p>Disponibilità: {p.stock}</p></div></main>}
